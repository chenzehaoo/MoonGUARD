#!/bin/sh
# Pre-commit hook for MoonGuard.
#
# Runs the same checks CI runs so a failing commit never lands upstream.
# Install with:
#   git config core.hooksPath .githooks

set -e

moon fmt --check
moon check