import {
  "chenzehaoo/moon_guard/lib/manifest",
}

options(
  name: "moon_guard/trust",
  version: "0.1.0",
  description: "Trusted key store with JSON and PEM persistence (RFC 7468). 支持 JSON 与 PEM（RFC 7468）持久化的可信密钥库",
)